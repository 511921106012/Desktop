#include<stdio.h>
int main()
{
    int  * stack= malloc(5 * sizeof(int));

    if(stack == NULL )
    {
        printf("meomory not allocated : ");
        return 0 ;

    }

    int n;
    scanf("%d",&n);

    int top = -1 

}